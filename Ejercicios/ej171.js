function mostrarValor() {
    console.log(valor);
    let valor = 5;
  }
mostrarValor();

//error al no estar definida la variable antes de llamarla para ser mostrada en la consola;
//El error se debe a que estás intentando acceder a la variable valor antes de que sea declarada. 
